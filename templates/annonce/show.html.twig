{% extends 'base.html.twig' %}

{% block title %}{{annonce.title}}{% endblock %}

{% block body %}
<div class="annonce-title" style="background-image: url({{  annonce.coverImage }})">
    <div class="container">
        <h1 class="my-3">{{annonce.title}}</h1>
        <p>{{annonce.intro}}</p>
        <p class="h2">
            <strong>{{annonce.rooms}} chambres</strong> pour <strong>{{annonce.price}}€ / nuit</strong>
        </p>
        <a href="#" class="btn btn-primary">Réserver !</a>
    </div>
</div>

<div class="container">
    <div class="row">
        <div class="col-md-8 mb-3">
            {{annonce.content | raw}}
            <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
                <ol class="carousel-indicators">
                    {% for image in annonce.images %}
                        <li data-target="#carouselExampleIndicators" data-slide-to="{{ loop.index0 }}" class="{% if loop.first %}active{% endif %}"></li>
                    {% endfor %}
                </ol>
                <div class="carousel-inner">
                    {% for image in annonce.images %}                    
                        <div class="carousel-item {% if loop.first %}active{% endif %}">
                            <img src="{{image.url}}" class="d-block w-100" alt="images">
                            <div class="carousel-caption d-none d-md-block">
                                <h5>{{annonce.title}}</h5>
                                <p>{{image.caption}}</p>
                            </div>
                        </div>
                    {% endfor %}
                </div>
                <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
        </div>
        <div class="col">
            <div class="row mb-3 align-items-center">
                <div class="col-4">
                    <img src="http://placehold.it/64x64" alt="John Doe">
                </div>
                <div class="col">
                    <h3>John Doe</h3>
                    <span class="badge badge-primary"> 3 annonces</span>
                </div>
            </div>
            <p>Lorem ipsum dolor sit, amet consectetur adipisicing elit. Perferendis debitis delectus quisquam recusandae eaque qui voluptatum ipsum id? Fugit, amet.</p>
            <p>Beatae ipsa assumenda iusto quod non! Similique, provident commodi. Error mollitia quis at aliquid, officia provident facilis incidunt ea cumque?</p>
            <p>Accusamus nostrum, modi delectus amet facere annonce illo hic aliquam eaque at corporis dolor eos voluptatem a optio dolorem cumque.</p>
        </div>
    </div>
</div>
{% endblock %}
