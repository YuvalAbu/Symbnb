{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<div class="container">
    <div class="jumbotron mt-3">
        <h1 class="display-3">Bienvenu sur SymBnB!</h1>
        <p class="lead">Nous mettons en realtion les voyageurs et le propriétaires qui veulent profiter d'opportunité et gagner de l'argent en proposnat leur bien en location!</p>
        <hr class="my-4">
        <p>Vous êtes voyageurs ? Et Vous voulez voir où vous pourrez loger pour votre prochain déplacement ?</p>
        <p class="lead">
            <a class="btn btn-primary btn-lg" href="#" role="button">Voir nos annconces</a>
        </p>
    </div>

    <h2 class="h1 my-5">Nos appartements stars</h2>
    <div class="row">
        {% for data in annonces %}
            {% include "annonce/_annonce.html.twig" with {'annonce': data.annonce} %}
        {% endfor %}
    </div>
    <h2 class="h1 my-5">Nos propriétaires stars</h2>
    <div class="row">
        {% for data in users %}
            <div class="col">
                <div class="card border-secondary mb-3">
                    <div class="card-body">
                        <h4 class="card-title">{{data.user.fullName}}</h4>
                        <hr>
                        <div class="card-text">
                            <img src="{{data.user.picture}}" alt="Avatar de {{data.user.fullName}}" class="float-left mr-3 avatar avatar-medium">
                            <p>{{data.user.intro}}</p>
                            <a href="{{ path('user_show', {'slug': data.user.slug }) }}" class="btn btn-success float-right">En savoir plus</a>
                        </div>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
